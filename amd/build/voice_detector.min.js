/**
 * Voice detection module — client-side continuous-speech detection.
 *
 * Detects one thing only: whether the student is speaking *continuously* for
 * longer than the quiz allows. There is no speaker identification, no
 * voiceprint enrolment, and no transcription — the microphone signal never
 * leaves the browser, and nothing but the duration/confidence of a flagged
 * episode is ever sent to the server.
 *
 * Key design decisions:
 * - Standalone module: decoupled from the video pipeline, follows the
 *   object_detector.js / gaze_tracker.js pattern.
 * - Features come from an AudioWorklet (js/vad_worklet.js) so frames are
 *   stamped with the audio clock and never dropped when the main thread
 *   stalls on FaceMesh/COCO-SSD inference. See that file for why this
 *   matters.
 * - Adaptive noise floor rather than a fixed level threshold: a fixed
 *   threshold is wrong for every room, and wrong again when the fan turns on
 *   halfway through. Speech is detected as a signal-to-noise excess over a
 *   continuously re-estimated background level.
 * - Duration *is* the debounce: rather than flagging any detected speech and
 *   then filtering it, a violation requires speech to run continuously past
 *   the configured limit. A cough, a sigh, or a short muttered remark can
 *   never reach it, so the flag can fire the instant the limit is crossed
 *   without a persistence counter of the kind gaze tracking needs.
 * - Gap tolerance: brief silences (natural pauses between words and
 *   sentences) do not reset the timer — only a real stretch of silence does.
 *   Without this, ordinary sentence rhythm would keep the timer near zero
 *   and nothing would ever be flagged.
 * - Graceful degradation: if the mic is unavailable or the worklet fails to
 *   load, the rest of proctoring continues unaffected.
 *
 * @module     quizaccess_proctor/voice_detector
 * @package    quizaccess_proctor
 * @copyright  2026 Ayush Kannaujiya
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define([],function(){let e={},n=null,o=null,t=null,i=null,r=null,c=!1,l=!1,a=null,u=null,s=[],d=null,f=0,h=!1,p=[],g=null,w=0,m=0,v=0,M=0,x=0,k=null,S=0,y=0,C={isViolation:!1,isSpeaking:!1,speechDuration:0,confidence:0};function b(){if(p.length<50)return!1;let e=0;for(let n=0;n<p.length;n++)e+=p[n];const n=e/p.length;if(n<=0)return!1;let o=0;for(let e=0;e<p.length;e++){const t=p[e]-n;o+=t*t}return o/=p.length,Math.sqrt(o)/n<.18}function D(){g=null,v=0,M=0}function V(e,n){C.isSpeaking=e,C.speechDuration=n,C.confidence=f}function A(){if(i){try{i.port.postMessage({command:"stop"}),i.disconnect()}catch(e){}i=null}if(r){try{r.disconnect()}catch(e){}r=null}if(t){try{t.disconnect()}catch(e){}t=null}if(o&&(o.getTracks().forEach(function(e){e.stop()}),o=null),n){try{n.close()}catch(e){}n=null}}return{init:async function(F,G){if(e=F||{},a="function"==typeof G?G:null,e.voiceMaxContinuousSpeech=e.voiceMaxContinuousSpeech||8,e.voiceGapToleranceMs=e.voiceGapToleranceMs||400,c)return!0;try{if(!e.voiceWorkletUrl)throw new Error("No worklet URL configured");if(!navigator.mediaDevices||!navigator.mediaDevices.getUserMedia)throw new Error("getUserMedia unavailable");o=await navigator.mediaDevices.getUserMedia({audio:{autoGainControl:!1,noiseSuppression:!1,echoCancellation:!1,channelCount:1},video:!1});const A=o.getAudioTracks()[0];if(A&&"function"==typeof A.getSettings){const e=A.getSettings();(e.autoGainControl||e.echoCancellation||e.noiseSuppression)&&window.console.warn("[Proctor VoiceDetector] Browser kept audio processing enabled (agc="+e.autoGainControl+" aec="+e.echoCancellation+" ns="+e.noiseSuppression+"). Speech levels will be normalised, which reduces detection reliability.")}const F=window.AudioContext||window.webkitAudioContext;if(!F)throw new Error("Web Audio API unavailable");if(n=new F,"suspended"===n.state&&await n.resume(),!n.audioWorklet)throw new Error("AudioWorklet unavailable");return await n.audioWorklet.addModule(e.voiceWorkletUrl),x=Date.now()-1e3*n.currentTime,t=n.createMediaStreamSource(o),i=new AudioWorkletNode(n,"proctor-vad-processor",{numberOfInputs:1,numberOfOutputs:1,outputChannelCount:[1],processorOptions:{frameMs:30}}),i.port.onmessage=function(n){const o=n.data;o&&"frame"===o.type&&function(n){if(!l)return;const o=n.rms,t=n.time;S++,1===S&&window.console.log("[Proctor VoiceDetector] Receiving audio frames — analysis running");null===u&&(u=o);p.push(o),p.length>50&&p.shift();const i=function(e){if(e.rms<.003)return!1;if(e.zcr>.45)return!1;const n=Math.max(u,1e-6);return 20*Math.log10(e.rms/n)>6}(n);i&&(m=t);s.push({loud:i,time:t}),s.length>33&&s.shift();let r=0,c=null;for(let e=0;e<s.length;e++)s[e].loud&&(r++,null===c&&(c=s[e].time));f=r/s.length,h?f<.1&&(h=!1):f>=.25&&(h=!0,d=c);b()&&(h=!1);const A=h;o<u?u+=.25*(o-u):h||(u+=.005*(o-u));if(e.voiceDebug&&t-y>=1){y=t;const i=Math.max(u,1e-6);window.console.log("[Proctor Voice] rms="+o.toFixed(4)+" floor="+i.toFixed(4)+" snr="+(20*Math.log10(o/i)).toFixed(1)+"dB zcr="+n.zcr.toFixed(2)+" loud="+(!!s.length&&s[s.length-1].loud)+" activity="+f.toFixed(2)+" speech="+A+" steady="+b()+" episode="+(null===g?"-":(t-g).toFixed(1)+"s")+" limit="+e.voiceMaxContinuousSpeech+"s")}!function(n,o){const t=e.voiceGapToleranceMs/1e3;if(n)null===g&&(g=null!==d?d:o,d=null,m=0,v=0,M=0),w=o,v+=f,M++;else if(null!==g&&o-w>t)return D(),void V(!1,0);if(null===g)return void V(!1,0);const i=Math.max(0,m-g);V(n,i),i>=e.voiceMaxContinuousSpeech&&function(e,n){const o=M>0?v/M:0;if(k={startedAt:Math.round(x+1e3*g),endedAt:Math.round(x+1e3*(g+n)),duration:n,confidence:o},C.isViolation=!0,g=e,w=e,v=0,M=0,a)try{a(k)}catch(e){window.console.error("[Proctor VoiceDetector] Violation callback failed:",e)}}(o,i)}(A,t)}(o)},r=n.createGain(),r.gain.value=0,t.connect(i),i.connect(r),r.connect(n.destination),c=!0,l=!0,!0}catch(e){return window.console.warn("[Proctor VoiceDetector] Init failed:",e),A(),c=!1,l=!1,!1}},getState:function(){return C.framesReceived=S,C},isReady:function(){return c},formatForReport:function(){return k?"talking,"+k.duration.toFixed(1)+","+k.confidence.toFixed(2):""},clearViolation:function(){C.isViolation=!1},stop:function(){l=!1,c=!1,A(),u=null,s=[],f=0,h=!1,d=null,p=[],k=null,S=0,y=0,D(),C={isViolation:!1,isSpeaking:!1,speechDuration:0,confidence:0}}}});