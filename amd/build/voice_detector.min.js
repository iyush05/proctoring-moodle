/**
 * Voice detection module — client-side continuous-speech detection.
 *
 * Detects one thing only: whether the student is speaking *continuously* for
 * longer than the quiz allows. There is no speaker identification, no
 * voiceprint enrolment, and no transcription — the microphone signal never
 * leaves the browser, and nothing but the duration/confidence of a flagged
 * episode is ever sent to the server.
 *
 * Key design decisions:
 * - Standalone module: decoupled from the video pipeline, follows the
 *   object_detector.js / gaze_tracker.js pattern.
 * - Features come from an AudioWorklet (js/vad_worklet.js) so frames are
 *   stamped with the audio clock and never dropped when the main thread
 *   stalls on FaceMesh/COCO-SSD inference. See that file for why this
 *   matters.
 * - Adaptive noise floor rather than a fixed level threshold: a fixed
 *   threshold is wrong for every room, and wrong again when the fan turns on
 *   halfway through. Speech is detected as a signal-to-noise excess over a
 *   continuously re-estimated background level.
 * - Duration *is* the debounce: rather than flagging any detected speech and
 *   then filtering it, a violation requires speech to run continuously past
 *   the configured limit. A cough, a sigh, or a short muttered remark can
 *   never reach it, so the flag can fire the instant the limit is crossed
 *   without a persistence counter of the kind gaze tracking needs.
 * - Gap tolerance: brief silences (natural pauses between words and
 *   sentences) do not reset the timer — only a real stretch of silence does.
 *   Without this, ordinary sentence rhythm would keep the timer near zero
 *   and nothing would ever be flagged.
 * - Graceful degradation: if the mic is unavailable or the worklet fails to
 *   load, the rest of proctoring continues unaffected.
 *
 * @module     quizaccess_proctor/voice_detector
 * @package    quizaccess_proctor
 * @copyright  2026 Ayush Kannaujiya
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define([],function(){let e={},n=null,o=null,t=null,i=null,r=null,c=!1,a=!1,l=null,u=null,s=[],d=0,f=!1,h=[],p=null,g=0,w=0,v=0,m=0,M=null,k=0,x=0,S={isViolation:!1,isSpeaking:!1,speechDuration:0,confidence:0};function y(){if(h.length<50)return!1;let e=0;for(let n=0;n<h.length;n++)e+=h[n];const n=e/h.length;if(n<=0)return!1;let o=0;for(let e=0;e<h.length;e++){const t=h[e]-n;o+=t*t}return o/=h.length,Math.sqrt(o)/n<.18}function C(){p=null,w=0,v=0}function b(e,n){S.isSpeaking=e,S.speechDuration=n,S.confidence=d}function D(){if(i){try{i.port.postMessage({command:"stop"}),i.disconnect()}catch(e){}i=null}if(r){try{r.disconnect()}catch(e){}r=null}if(t){try{t.disconnect()}catch(e){}t=null}if(o&&(o.getTracks().forEach(function(e){e.stop()}),o=null),n){try{n.close()}catch(e){}n=null}}return{init:async function(V,A){if(e=V||{},l="function"==typeof A?A:null,e.voiceMaxContinuousSpeech=e.voiceMaxContinuousSpeech||8,e.voiceGapToleranceMs=e.voiceGapToleranceMs||400,c)return!0;try{if(!e.voiceWorkletUrl)throw new Error("No worklet URL configured");if(!navigator.mediaDevices||!navigator.mediaDevices.getUserMedia)throw new Error("getUserMedia unavailable");o=await navigator.mediaDevices.getUserMedia({audio:{autoGainControl:!1,noiseSuppression:!1,echoCancellation:!1,channelCount:1},video:!1});const D=o.getAudioTracks()[0];if(D&&"function"==typeof D.getSettings){const e=D.getSettings();(e.autoGainControl||e.echoCancellation||e.noiseSuppression)&&window.console.warn("[Proctor VoiceDetector] Browser kept audio processing enabled (agc="+e.autoGainControl+" aec="+e.echoCancellation+" ns="+e.noiseSuppression+"). Speech levels will be normalised, which reduces detection reliability.")}const V=window.AudioContext||window.webkitAudioContext;if(!V)throw new Error("Web Audio API unavailable");if(n=new V,"suspended"===n.state&&await n.resume(),!n.audioWorklet)throw new Error("AudioWorklet unavailable");return await n.audioWorklet.addModule(e.voiceWorkletUrl),m=Date.now()-1e3*n.currentTime,t=n.createMediaStreamSource(o),i=new AudioWorkletNode(n,"proctor-vad-processor",{numberOfInputs:1,numberOfOutputs:1,outputChannelCount:[1],processorOptions:{frameMs:30}}),i.port.onmessage=function(n){const o=n.data;o&&"frame"===o.type&&function(n){if(!a)return;const o=n.rms,t=n.time;k++,1===k&&window.console.log("[Proctor VoiceDetector] Receiving audio frames — analysis running");null===u&&(u=o);h.push(o),h.length>50&&h.shift();s.push(function(e){if(e.rms<.003)return!1;if(e.zcr>.45)return!1;const n=Math.max(u,1e-6);return 20*Math.log10(e.rms/n)>6}(n)),s.length>33&&s.shift();let i=0;for(let e=0;e<s.length;e++)s[e]&&i++;d=i/s.length,f?d<.15&&(f=!1):d>=.35&&(f=!0);y()&&(f=!1);const r=f;o<u?u+=.25*(o-u):f||(u+=.005*(o-u));if(e.voiceDebug&&t-x>=1){x=t;const i=Math.max(u,1e-6);window.console.log("[Proctor Voice] rms="+o.toFixed(4)+" floor="+i.toFixed(4)+" snr="+(20*Math.log10(o/i)).toFixed(1)+"dB zcr="+n.zcr.toFixed(2)+" loud="+(!!s.length&&s[s.length-1])+" activity="+d.toFixed(2)+" speech="+r+" steady="+y()+" episode="+(null===p?"-":(t-p).toFixed(1)+"s")+" limit="+e.voiceMaxContinuousSpeech+"s")}!function(n,o){const t=e.voiceGapToleranceMs/1e3;if(n)null===p&&(p=o,w=0,v=0),g=o,w+=d,v++;else if(null!==p&&o-g>t)return C(),void b(!1,0);if(null===p)return void b(!1,0);const i=o-p;b(n,i),i>=e.voiceMaxContinuousSpeech&&function(e,n){const o=v>0?w/v:0;if(M={startedAt:Math.round(m+1e3*p),endedAt:Math.round(m+1e3*e),duration:n,confidence:o},S.isViolation=!0,p=e,g=e,w=0,v=0,l)try{l(M)}catch(e){window.console.error("[Proctor VoiceDetector] Violation callback failed:",e)}}(o,i)}(r,t)}(o)},r=n.createGain(),r.gain.value=0,t.connect(i),i.connect(r),r.connect(n.destination),c=!0,a=!0,!0}catch(e){return window.console.warn("[Proctor VoiceDetector] Init failed:",e),D(),c=!1,a=!1,!1}},getState:function(){return S.framesReceived=k,S},isReady:function(){return c},formatForReport:function(){return M?"talking,"+M.duration.toFixed(1)+","+M.confidence.toFixed(2):""},clearViolation:function(){S.isViolation=!1},stop:function(){a=!1,c=!1,D(),u=null,s=[],d=0,f=!1,h=[],M=null,k=0,x=0,C(),S={isViolation:!1,isSpeaking:!1,speechDuration:0,confidence:0}}}});