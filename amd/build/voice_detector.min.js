/**
 * Voice detection module — client-side continuous-speech detection.
 *
 * Detects one thing only: whether the student is speaking *continuously* for
 * longer than the quiz allows. There is no speaker identification, no
 * voiceprint enrolment, and no transcription — the microphone signal never
 * leaves the browser, and nothing but the duration/confidence of a flagged
 * episode is ever sent to the server.
 *
 * Key design decisions:
 * - Standalone module: decoupled from the video pipeline, follows the
 *   object_detector.js / gaze_tracker.js pattern.
 * - Features come from an AudioWorklet (js/vad_worklet.js) so frames are
 *   stamped with the audio clock and never dropped when the main thread
 *   stalls on FaceMesh/COCO-SSD inference. See that file for why this
 *   matters.
 * - Adaptive noise floor rather than a fixed level threshold: a fixed
 *   threshold is wrong for every room, and wrong again when the fan turns on
 *   halfway through. Speech is detected as a signal-to-noise excess over a
 *   continuously re-estimated background level.
 * - Duration *is* the debounce: rather than flagging any detected speech and
 *   then filtering it, a violation requires speech to run continuously past
 *   the configured limit. A cough, a sigh, or a short muttered remark can
 *   never reach it, so the flag can fire the instant the limit is crossed
 *   without a persistence counter of the kind gaze tracking needs.
 * - Gap tolerance: brief silences (natural pauses between words and
 *   sentences) do not reset the timer — only a real stretch of silence does.
 *   Without this, ordinary sentence rhythm would keep the timer near zero
 *   and nothing would ever be flagged.
 * - Graceful degradation: if the mic is unavailable or the worklet fails to
 *   load, the rest of proctoring continues unaffected.
 *
 * @module     quizaccess_proctor/voice_detector
 * @package    quizaccess_proctor
 * @copyright  2026 Ayush Kannaujiya
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define([],function(){const e=.25;let n={},o=null,t=null,i=null,r=null,c=null,a=!1,l=!1,u=null,s=null,d=0,f=[],h=null,p=0,g=0,w=0,v=0,m=null,M=0,x=0,k={isViolation:!1,isSpeaking:!1,speechDuration:0,confidence:0};function y(){if(f.length<50)return!1;let e=0;for(let n=0;n<f.length;n++)e+=f[n];const n=e/f.length;if(n<=0)return!1;let o=0;for(let e=0;e<f.length;e++){const t=f[e]-n;o+=t*t}return o/=f.length,Math.sqrt(o)/n<.18}function C(){h=null,g=0,w=0}function D(e,n){k.isSpeaking=e,k.speechDuration=n,k.confidence=d}function S(){if(r){try{r.port.postMessage({command:"stop"}),r.disconnect()}catch(e){}r=null}if(c){try{c.disconnect()}catch(e){}c=null}if(i){try{i.disconnect()}catch(e){}i=null}if(t&&(t.getTracks().forEach(function(e){e.stop()}),t=null),o){try{o.close()}catch(e){}o=null}}return{init:async function(V,b){if(n=V||{},u="function"==typeof b?b:null,n.voiceMaxContinuousSpeech=n.voiceMaxContinuousSpeech||8,n.voiceGapToleranceMs=n.voiceGapToleranceMs||400,a)return!0;try{if(!n.voiceWorkletUrl)throw new Error("No worklet URL configured");if(!navigator.mediaDevices||!navigator.mediaDevices.getUserMedia)throw new Error("getUserMedia unavailable");t=await navigator.mediaDevices.getUserMedia({audio:{autoGainControl:!1,noiseSuppression:!1,echoCancellation:!0,channelCount:1},video:!1});const S=window.AudioContext||window.webkitAudioContext;if(!S)throw new Error("Web Audio API unavailable");if(o=new S,"suspended"===o.state&&await o.resume(),!o.audioWorklet)throw new Error("AudioWorklet unavailable");return await o.audioWorklet.addModule(n.voiceWorkletUrl),v=Date.now()-1e3*o.currentTime,i=o.createMediaStreamSource(t),r=new AudioWorkletNode(o,"proctor-vad-processor",{numberOfInputs:1,numberOfOutputs:1,outputChannelCount:[1],processorOptions:{frameMs:30}}),r.port.onmessage=function(o){const t=o.data;t&&"frame"===t.type&&function(o){if(!l)return;const t=o.rms,i=o.time;M++,1===M&&window.console.log("[Proctor VoiceDetector] Receiving audio frames — analysis running");null===s&&(s=t);f.push(t),f.length>50&&f.shift();const r=function(e){if(e.rms<.003)return 0;if(e.zcr>.45)return 0;const n=Math.max(s,1e-6),o=20*Math.log10(e.rms/n);if(o<=6)return 0;if(o>=12)return 1;return(o-6)/6}(o);d=e*r+.75*d;const c=d>.5&&!y();t<s?s+=.25*(t-s):c||(s+=.005*(t-s));if(n.voiceDebug&&i-x>=1){x=i;const e=Math.max(s,1e-6);window.console.log("[Proctor Voice] rms="+t.toFixed(4)+" floor="+e.toFixed(4)+" snr="+(20*Math.log10(t/e)).toFixed(1)+"dB zcr="+o.zcr.toFixed(2)+" score="+d.toFixed(2)+" speech="+c+" steady="+y()+" episode="+(null===h?"-":(i-h).toFixed(1)+"s")+" limit="+n.voiceMaxContinuousSpeech+"s")}!function(e,o){const t=n.voiceGapToleranceMs/1e3;if(e)null===h&&(h=o,g=0,w=0),p=o,g+=d,w++;else if(null!==h&&o-p>t)return C(),void D(!1,0);if(null===h)return void D(!1,0);const i=o-h;D(e,i),i>=n.voiceMaxContinuousSpeech&&function(e,n){const o=w>0?g/w:0;if(m={startedAt:Math.round(v+1e3*h),endedAt:Math.round(v+1e3*e),duration:n,confidence:o},k.isViolation=!0,h=e,p=e,g=0,w=0,u)try{u(m)}catch(e){window.console.error("[Proctor VoiceDetector] Violation callback failed:",e)}}(o,i)}(c,i)}(t)},c=o.createGain(),c.gain.value=0,i.connect(r),r.connect(c),c.connect(o.destination),a=!0,l=!0,!0}catch(e){return window.console.warn("[Proctor VoiceDetector] Init failed:",e),S(),a=!1,l=!1,!1}},getState:function(){return k.framesReceived=M,k},isReady:function(){return a},formatForReport:function(){return m?"talking,"+m.duration.toFixed(1)+","+m.confidence.toFixed(2):""},clearViolation:function(){k.isViolation=!1},stop:function(){l=!1,a=!1,S(),s=null,d=0,f=[],m=null,M=0,x=0,C(),k={isViolation:!1,isSpeaking:!1,speechDuration:0,confidence:0}}}});